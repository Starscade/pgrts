{
	"exports": "./src/mod.ts",
	"fmt": {
		"lineWidth": 80,
		"semiColons": false,
		"singleQuote": true,
		"useTabs": true
	},
	"license": "MIT",
	"lock": false,
	"name": "@starscade/postgrets",
	"tasks": {
		"check": "deno lint src && deno fmt src && deno check src",
		"make": "deno task check && deno bundle --minify --output esm/postgrets.esm.js --platform browser src/mod.ts",
		"test": "deno run --allow-env --allow-import --allow-net --env-file --unsafely-ignore-certificate-errors src/test.ts"
	},
	"version": "0.1.0"
}
