{
	"exports": "./src/mod.ts",
	"fmt": {
		"lineWidth": 80,
		"semiColons": false,
		"singleQuote": true,
		"useTabs": true
	},
	"license": "MIT",
	"lock": false,
	"name": "@starscade/postgrets",
	"tasks": {
		"bundle": "deno task check && deno bundle --minify --output esm/postgrets.esm.js --platform browser src/mod.ts",
		"check": "deno lint src && deno fmt --check src && deno check src",
		"test": "deno run --allow-env --allow-import --allow-net --env-file src/test.ts"
	},
	"version": "0.1.0"
}
