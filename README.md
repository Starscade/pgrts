# PostgreTS

A plug-and-play [PostgREST](https://docs.postgrest.org/) ESM module. Use `deno task make` to transpile into JavaScript, or test queries on the command line via `deno task test <pgrst_query>`.

Use instantly via `import PostgREST from 'https://esm.sh/gh/Starscade/PostgreTS'`.
